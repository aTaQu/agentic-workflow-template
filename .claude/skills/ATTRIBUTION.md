# Vendored skills — attribution

These skills are vendored copies, kept in-repo so the template is self-contained (ADR 0001).

## From Matt Pocock's skills (MIT) — https://github.com/mattpocock/skills

`tdd`, `grill-with-docs`, `grill-me`, `handoff`, `prototype`, `teach`,
`improve-codebase-architecture`, `diagnose`.

Licensed MIT © Matt Pocock — see [`LICENSE-mattpocock`](LICENSE-mattpocock) in this directory.

## The project's own

`parallel-batch` — authored for this template as a stack-neutral parallel-implementation engine
(the upstream pattern was PHP/Symfony-coupled; this version reads the project's `Verify` hook from
`AGENTS.md`).

---

To re-sync the MP skills with upstream: re-clone `github.com/mattpocock/skills` and copy the skill
directories over. The local copies drift from upstream over time — that's the accepted cost of a
self-contained template (ADR 0001).
