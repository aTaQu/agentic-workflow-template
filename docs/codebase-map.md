# Codebase Map

Where things live, organized by phase. `/task-done` and `/phase-done` keep this current as tasks add
files. One line per entry — this is a navigation index, not documentation.

## Phase 1 — [name]
_(not started)_

<!-- Example of the shape this takes as the project grows:

## Phase 2 — Notifications  (complete)
- `src/notifications/email/` — email channel (configured mailer)
- `src/notifications/sms/` — SMS channel
- `src/notifications/prefs/` — preferences UI
-->
